{
  "name": "clock-challenge",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "prettier": "npx prettier --write --log-level silent .",
    "tsc": "tsc --project tsconfig.json",
    "check": "npm run prettier && npm run tsc"
  },
  "devDependencies": {
    "prettier": "3.0.0",
    "typescript": "^5.0.2",
    "vite": "^4.4.5"
  }
}
